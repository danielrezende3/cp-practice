{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "lldb-dap: Launch",
      "type": "lldb-dap",
      "request": "launch",
      "program": "${fileDirname}/.build/${fileBasenameNoExtension}",
      "cwd": "${fileDirname}",
      "args": [],
      "preLaunchTask": "C/C++: build active file"
    },
    {
      "name": "lldb-dap: Launch with input",
      "type": "lldb-dap",
      "request": "launch",
      "program": "${fileDirname}/.build/${fileBasenameNoExtension}",
      "cwd": "${fileDirname}",
      "args": [],
      "stdio": [
        "${fileDirname}/${fileBasenameNoExtension}.input",
      ],
      "preLaunchTask": "C/C++: build active file"
    }
  ]
}
